# regi_chat
POC for press-to-talk (SST) and TTS AI chat

flutter run --dart-define-from-file=.env

# developer workstation (Local) PWA launch

flutter run -d web-server --web-port=5000 --web-hostname=0.0.0.0 --dart-define-from-file=.env

Note:  Open browser, http://localhost:5000   

# netlify 
# (dev)


